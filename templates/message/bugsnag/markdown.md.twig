🐞*Bugsnag*

New error from *{{ error.project }}*:

```
{{ error.exceptionClass }}
	{{ error.message }}
```

*First occurrence time:* {{ error.firstReceived|date('Y-m-d H:i e') }}
*Occurrence time:* {{ error.receivedAt|date('Y-m-d H:i e') }}

[Show error details]({{ error.url }})
